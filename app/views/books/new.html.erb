<h1>New book</h1>

<%= form_with model: @book do |f| %>

  <h4>Title(ショップ名)</h4>
  <%= f.text_field :title %>

  <h4>Opinion(説明)</h4>
  <%= f.text_area :body %><br>

  <%= f.submit 'Create Book' %>
<% end %>
